# Remit — Warp project rules
Canonical rules and the build roadmap live in **AGENTS.md** at the repo root; read it first. TL;DR:
employer-side WFH/WHS/outcome **governance**, never employee surveillance. `src/domain/**` is pure and
tested; services/API/CLI sit on top; everything is jurisdiction-parameterised and audit-logged; all
legal references are counsel-verified scaffolds. Run `npm run check` before finishing a task.
